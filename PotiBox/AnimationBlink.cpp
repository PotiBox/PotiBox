#include "AnimationBlink.h"
#include "Debug.h"

AnimationBlink::AnimationBlink() {
}

void AnimationBlink::cycle(unsigned long currentTime) {
  /*blinkcolor = _color;

  for (int i =0; i < nb; i++) {
    setColor(getColorFromInt(blinkcolor));
    delay(wait);
    _color = OFF;
    setColor(off());
    delay(wait);
  }*/
}